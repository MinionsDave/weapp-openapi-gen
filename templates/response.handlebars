/* tslint:disable */
/* eslint-disable */
/**
 * Constrains the http response to not have the body defined as `T | null`, but `T` only.
 */
export type {{ responseClass }}<T> = {{weappNamespace}}.RequestSuccessCallbackResult & {
  data: T
}
